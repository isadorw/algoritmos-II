// exemplo de comando de atribuicao
#include <stdio.h>

int main() // função principal
{
	int horas;
	float salario;
	double dias, desconto;
	char nome[50] = "Vanessa Campos";
	char sexo;
	
	horas = 36; // atribuido 36 a var horas
	salario = 1200.34; // atribuido valor do sal a var salario
	dias = (double) horas / 24;
	sexo = 'F';
	printf("Imprimindo valores");
}